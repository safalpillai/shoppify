import { ModifyUsernameDirective } from './modify-username.directive';

describe('ModifyUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyUsernameDirective();
    expect(directive).toBeTruthy();
  });
});

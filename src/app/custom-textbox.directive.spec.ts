import { CustomTextboxDirective } from './custom-textbox.directive';

describe('CustomTextboxDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTextboxDirective();
    expect(directive).toBeTruthy();
  });
});
